@model JScroll.Controllers.UserModel


<table class="table">


@foreach (var item in Model.Users) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>

    </tr>
}
    <a href="/home/Users?pageID=@Model.PageCount" data-ui="jscroll-next">Next</a>
</table>
